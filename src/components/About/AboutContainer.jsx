import cx from "classnames";

// ================================================

const AboutContainer = ({ children }) => {
  return <div className={cx("about-container")}>{children}</div>;
};

export default AboutContainer;
